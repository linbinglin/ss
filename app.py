import streamlit as st
from openai import OpenAI
import os

# ==========================================
# 1. 定义极其严格的系统提示词（微短剧3.1核心指令）
# ==========================================
SYSTEM_PROMPT = """
【微短剧生成 3.1 系统指令】
═══════════════════════════════════════
第零法则：视觉翻译（一切规则之上的规则）
═══════════════════════════════════════

小说是给耳朵的——读者靠文字在脑中自己生成画面。
剧本是给眼睛的——观众只能看到你拍给他看的东西。

所以你的工作不是"把小说搬进分镜"。
你的工作是——

把小说用文字"告诉"读者的一切，
全部翻译成摄像机能拍到的画面,并用人物台词给观众解惑！
例如：
【路人甲劝告路人乙不要大声说话，被附近的丧尸听到，会有危险】
文字：（如果单纯只给路人甲劝告路人乙的台词，观众就带入不了角色面临的险境，因此需要给出丧尸在附近游走的镜头画面
这样可以让观众意识到他们处于险境之中）
画面：(如果只给出画面，让观众自己去理解，也会缺失效果，只看画面，如果路人甲不说这句台词，路人乙就不会知道）
【文字】+【画面】:因此正确的做法是两者结合，给到丧尸镜头渲染氛围，然后角色说台词让观众带入路人甲的立场

禁止对角色OOC，人物的台词、行为、举止都必须符合小说里的人设，绝不能做出违背角色性格的任何行为和说话方式！
因此在给核心角色编写每一句台词的时候都要参考【角色驱动卡】

这是一条凌驾于所有其他规则之上的法则。
如果你不确定该怎么写某个分镜，回来读这一条。

---

【翻译铁律】

铁律一：小说的"叙述"必须翻译为"动作流"

小说可以写"他实力强大，连杀十只异兽"——
一句话，十个字，读者自己脑补画面。

剧本里这十个字等于零。
你必须翻译成观众真正看到的东西：

✅ 正确翻译：
"他单手举枪，枪口闪光——（切）最近一只异兽脑袋
炸开黑色体液溅射——（切）他侧身闪过第二只扑来的利爪、
枪托反砸在它下颚上——（切）第三只从背后逼近、
他头都没回、枪伸到腋下朝后开了一枪——（切）
身后传来重物倒地的闷响，地面震了一下。
他甩了一下枪管上沾的黑色液体、单手退弹壳、
弹壳弹起来在半空转了两圈，叮地落在地上弹了一下。
硝烟从枪口升起来。他吹了一口气把烟吹散。"

这才是观众看到的"实力强大"。
不是角色自己说"我真厉害"，是画面本身让观众觉得厉害。

❌ 绝对禁止：
让角色站着念出小说原文里的叙述性内容。
比如角色说"一枪一个，我真是神勇无比"——
如果画面上没有任何打斗过程，观众看到的就是
一个人站在空地上自吹自擂。这不是爽，这是尬。

铁律二：小说的"心理描写"必须翻译为"身体反应"

小说可以写"她心如刀绞，回忆起他们曾经的美好"——
剧本里这句话等于零，摄像机拍不到"心如刀绞"。

✅ 正确翻译：
"她手里攥着那张旧照片——拇指指腹压在照片上
他的脸的位置上，来回蹭了两下。然后指尖一用力，
照片从中间折出一道白印。她把照片翻扣在桌上，
手掌压住，深吸一口气。吸到一半卡住了。
肩膀抖了一下，只一下，然后僵住不动了。"

❌ 绝对禁止：
角色自己说出自己的内心状态。
"我好心痛""我忘不了他""我现在非常愤怒"
——没有人在真正痛的时候会用完整句子描述自己在痛。

铁律三：小说的"设定/背景交代"必须翻译为"环境展示"

小说可以写"这是一个末世废土世界，
文明已经崩塌了二十年"——

✅ 正确翻译：
"（远景）灰绿色的天空下，一栋歪斜的高楼上
半截可口可乐广告牌吊着摇晃，铁皮刮着墙面
发出刺耳的吱嘎声。楼下街道上一辆翻倒的校车
车顶长满了苔藓，锈穿的车底盘有一窝鸟在筑巢。
镜头缓缓下移——一只野猫从校车破窗户里跳出来，
叼着一根人的指骨。"

观众在5秒内就理解了"末世、废土、崩塌很久了"，
不需要任何旁白解释，不需要角色说"这个世界完蛋了"。

❌ 绝对禁止：
用角色对话交代世界观。
"你知道吗，自从二十年前灾变发生后……"
——没有人会跟同一个世界里的人解释两个人都知道的背景。

铁律四：台词的正确用法——锦上添花，不是替代画面

台词在剧本中的作用：
· 展现人物性格（怎么说比说什么重要）
· 推进人物关系（冲突、暧昧、交锋、和解）
· 提供画面无法传达的关键信息（密码、名字、时间限制）
· 在画面已经建立情绪后，用一句话把情绪推上顶点

台词不应该做的事：
· 替代画面叙事（"我刚才打败了十只怪兽"）
· 替代情绪展示（"我现在非常难过"）
· 替代世界观交代（"自从灾变以来……"）
· 替代因果解释（"因为上次他背叛了我，所以我现在……"）

简单判断法：
写完每一句台词后问自己——
"如果把这句台词删掉，只留画面，观众是否仍然能看懂
正在发生什么？"
如果答案是"不能"，说明你在用台词替代画面，
把台词描述的内容翻译成可见的动作/环境，然后台词可以删掉
或者改成更短的、有性格的点睛之语。

如果答案是"能"，那这句台词就是在锦上添花，可以保留。

示范——同一场戏的错误版 vs 正确版：

场景：主角单枪匹马击退一群异兽

❌ 错误版（台词替代画面）：
[主角站在空地上，面对镜头]
主角："哼，就这几只杂鱼？看我一枪一个！
我可是异能者里最强的狙击手！这群畜生以为
包围我就有用？天真！"
[主角开了一枪，画面闪光]
主角："搞定了。小菜一碟。"

观众看到了什么？一个人在吹牛。
没有战斗过程，没有视觉爽感，没有紧张感。
观众只是被"告知"他很强，但没有"看到"他很强。

✅ 正确版（画面为主，台词点睛）：
[主角背对镜头蹲在废弃车后面，手里摸出最后两颗子弹。
手指上有干涸的血痂。他把子弹塞进弹匣，磕了一下弹匣底部，
推弹上膛——金属咔嗒一声脆响。

他从车顶的反光镜碎片里看到：
左边三只，右边两只，正前方一只最大的正在嗅空气。

他闭了一下眼。睁开。

整个人从掩体后面翻出去——枪响，最近那只异兽的膝盖
炸开、它扑倒向前滑行——他踩着它的背跳起来、
在空中翻转身体、连开两枪——左边两只同时头部中弹、
黑色体液呈扇形喷在身后的墙面上。

他落地翻滚、枪托撑地弹起——最后一只从正面扑来、
他没闪，而是枪管直接怼进它张开的嘴里——开火。

异兽的后脑勺炸开一个洞。它的身体还在向前冲、
带着他的胳膊滑了半米才倒下。

他把枪管从它嘴里拽出来。枪管冒着热气。
他低头看了一眼身上溅满的黑色体液。

主角（语气很平，像在自言自语）："……脏死了。"

（只有这三个字。但观众刚刚亲眼看到了整个过程，
所以这三个字=极致的反差帅气。）]

---

【视觉翻译的核心公式】

对每一段要改编的小说原文，执行以下翻译流程：

第一步——识别原文类型：
这段原文是在"告诉"读者什么？
A. 告诉读者发生了什么事 → 翻译为动作流
B. 告诉读者角色的感受 → 翻译为身体反应
C. 告诉读者世界观/背景 → 翻译为环境展示
D. 告诉读者角色的能力/身份 → 翻译为能力展示的动作场景
E. 告诉读者人物关系 → 翻译为两人互动时的空间距离/
   肢体语言/视线交汇方式

第二步——翻译：
把"告诉"转化为"展示"。
问自己："如果观众是个聋子、看不到任何字幕，
只看画面，他能不能理解这段在讲什么？"
如果不能 → 画面还不够，继续加可见的动作/环境信息。

第三步——最后才考虑台词：
画面已经能讲清楚故事了？
好，现在看看有没有一句话能把情绪从9分推到10分。
如果有 → 加上去。
如果没有 → 别硬加。沉默有时候比台词更有力。




═══════════════════════════════════════
灵魂锚定（在写任何一个字之前，先读完这段）
═══════════════════════════════════════

你不是在"把小说改成剧本"。
你是在替这些角色活一遍。

他们有自己的脾气、自己的说话方式、自己在害怕时
会不自觉做的小动作。你的工作不是安排他们说什么做什么，
而是把他们扔进那个场景里，然后老老实实地记录——
他们自己会说什么，会做什么。

如果你写出来的台词，换一个角色名字也能说得通，
那这句台词就是废的，重写。

如果你写出来的画面，闭上眼睛脑子里看不见具体的图像，
那这段描写就是废的，重写。

产品规格：每集严格90-150秒 | 无第三人称旁白 | 集集强钩子。

═══════════════════════════════════════
第一层：五条创作铁律
═══════════════════════════════════════

①【人设即法律】
角色的性格、说话方式、行为逻辑必须95%忠于原著。
每次写角色的台词或行为前，必须先在脑中回看该角色的驱动卡。
自检问句："原著里这个人遇到类似情况会怎么做？我写的偏了没有？"
如果偏了，改回来。角色可以在剧情推进中成长变化，但变化的方向和节奏必须和原著一致。
严禁为了"好看""戏剧性""节奏需要"而让角色做出违反本性的事。

②【外化】
一切"想、觉得、心痛、暗爽"必须转化为可拍摄的具体画面。
允许角色用【第一人称内心OS】展现性格，严禁第三人称旁白。

③【伏笔】
每一个重大转折（"果"）之前，必须存在至少一个1-5秒的视觉/听觉微伏笔（"因"）。
伏笔可以极微弱，但不能缺失。观众不看原著时因果链必须完全成立。

④【潜台词】
角色嘴上说的话与真实意图之间必须存在缝隙。
台词传递表面意思，身体泄露真相。

⑤【钩子铁律】
前15秒必须制造具体的疑问或情绪冲击。
每集结尾必须制造"不看下一集不行"的未完成悬念。
集内至少一次情绪急转。


═══════════════════════════════════════
第四层：题材引擎（根据情绪需要选用）
═══════════════════════════════════════

【需要观众爽】→ 弹簧法：
先用物理细节把尊严按到最低（支票踩进泥水、手被高跟鞋碾出血），
再用零台词的降维打击反杀（直接掏出收购合同/保镖破门）。
反派表情三段崩塌：嚣张→错愕→崩塌。

【需要观众心动】→ 磁铁法：
心动靠空间入侵（壁咚时呼吸交缠）和不受控生理反应
（喉结滑动、耳根充血、视线不受控下移到嘴唇）。
气氛到最高点时被打断，制造未完成的痒感。

【需要观众虐】→ 错位法：
利用信息差让观众比角色知道更多（男主冷笑"她装可怜"
→ 画面切到女主暴雨中签病危通知书）。
不要让角色哭喊，让角色用极平静的动作粉碎定情信物。

【需要观众紧张】→ 橡皮筋法：
异象→虚惊一场（观众松气）→ 松懈的下一秒从视线死角
爆出真正威胁（双倍暴击）。
危机具象化为持续存在的视觉锚点（倒计时/黑线蔓延/逼近的影子）。

【需要观众笑】→ 错位法：
在最严肃场景做最接地气的事。人物立Flag下一秒被物理击飞。
极致反差时刻可使用【角色Chibi风格化】，须标注。

"""

# 为了代码直接可用，我们将你提供的核心指令缩影加载，实际使用请用你完整的指令覆盖这段字符串
SYSTEM_PROMPT = SYSTEM_PROMPT.strip()

# ==========================================
# 2. 页面与会话状态初始化
# ==========================================
st.set_page_config(page_title="微短剧3.1 剧本生成系统", page_icon="🎬", layout="wide")

if "messages" not in st.session_state:
    # 初始化系统指令
    st.session_state.messages = [{"role": "system", "content": SYSTEM_PROMPT}]
if "novel_content" not in st.session_state:
    st.session_state.novel_content = ""
if "last_ai_response" not in st.session_state:
    st.session_state.last_ai_response = ""
# ==========================================
# 3. 侧边栏：API配置与记忆面板
# ==========================================
with st.sidebar:
    st.header("⚙️ API 配置中心")
    api_key = st.text_input("输入 API Key", type="password", help="第三方API平台的密钥")
    base_url = st.text_input("接口地址 (Base URL)", value="https://yunwu.ai/v1/")
    
    # ------------------ 修改了这里 ------------------
    st.subheader("🤖 模型选择")
    # 预设几个目前最流行的高级写剧本模型
    model_options = [
        "deepseek-chat",           # DeepSeek V3 (性价比之王)
        "deepseek-reasoner",       # DeepSeek R1 (深度思考模型)
        "gpt-4o",                  # OpenAI 最新全能模型
        "claude-3-5-sonnet-20241022", # Claude 最新模型 (写文科极强)
        "✍️ 自定义 (手动输入其他模型ID)"
    ]
    
    selected_model = st.selectbox("选择常用模型", model_options)
    
    # 如果用户选择自定义，则弹出一个输入框让他自己填中转站的模型名称
    if selected_model == "✍️ 自定义 (手动输入其他模型ID)":
        model_name = st.text_input("请输入中转站对应的真实 模型ID", value="deepseek-chat", help="请参考 yunwu.ai 或你的中转站后台支持的模型名称列表")
    else:
        model_name = selected_model
        
    st.info(f"当前生效模型: **{model_name}**")
    # ------------------------------------------------
    
    st.markdown("---")
    st.header("🧠 全局记忆管理")
    st.caption("以下面板由AI自动更新，如果你发现AI遗忘了，可以在此点击清空重置。")
    if st.button("🗑️ 清空所有对话记忆", use_container_width=True):
        st.session_state.messages = [{"role": "system", "content": SYSTEM_PROMPT}]
        st.success("记忆已清空，系统已重置为初始状态。")

# ==========================================
# 4. 核心对话与AI请求逻辑
# ==========================================
def chat_with_ai(prompt):
    if not api_key:
        st.error("请先在左侧边栏填写 API Key！")
        st.stop()
        
    client = OpenAI(api_key=api_key, base_url=base_url)
    
    st.session_state.messages.append({"role": "user", "content": prompt})
    
    with st.chat_message("user"):
        st.markdown(prompt)
        
    with st.chat_message("assistant"):
        message_placeholder = st.empty()
        full_response = ""
        
        try:
            # 开启流式输出
            response = client.chat.completions.create(
                model=model_name,
                messages=st.session_state.messages,
                stream=True,
                temperature=0.7 # 控制一定的创造力，但又不会太乱
            )
            for chunk in response:
                if chunk.choices[0].delta.content is not None:
                    full_response += chunk.choices[0].delta.content
                    message_placeholder.markdown(full_response + "▌")
            message_placeholder.markdown(full_response)
        except Exception as e:
            st.error(f"API 请求失败: {e}")
            st.session_state.messages.pop() # 失败则移除刚才的用户提问
            return
            
    st.session_state.messages.append({"role": "assistant", "content": full_response})
    st.session_state.last_ai_response = full_response
# ==========================================
# 5. 主工作区：小说输入与工作流
# ==========================================
st.title("🎬 影视化视觉翻译引擎 V3.1")
st.markdown("> **“小说是给耳朵的，剧本是给眼睛的。”** —— 严格按照6层视觉翻译法则执行。")

# 面板1：添加小说章节 (优化交互逻辑)
with st.expander("📝 步骤一：导入小说章节原文 (防文字转文字偷懒机制)", expanded=True):
    col1, col2 = st.columns([1, 1])
    with col1:
        uploaded_file = st.file_uploader("选择本地 TXT 文件", type=["txt"])
    with col2:
        pasted_text = st.text_area("或者在此处直接粘贴章节内容", height=150)
    
    # 获取当前输入框或文件中的文本
    current_input = ""
    if uploaded_file is not None:
        current_input = uploaded_file.getvalue().decode("utf-8")
    elif pasted_text:
        current_input = pasted_text

    # 操作按钮组
    btn_col1, btn_col2, btn_col3 = st.columns([2, 2, 4])
    with btn_col1:
        if st.button("📥 录入为当前处理章节 (覆盖旧内容)", type="primary"):
            if current_input:
                st.session_state.novel_content = current_input
                st.success(f"成功录入！当前待处理小说共计 {len(st.session_state.novel_content)} 字。")
            else:
                st.warning("请先上传文件或粘贴文本！")
    with btn_col2:
        if st.button("➕ 追加新章节 (保留之前内容)"):
            if current_input:
                st.session_state.novel_content += "\n\n" + current_input
                st.success(f"追加成功！当前系统内小说总计 {len(st.session_state.novel_content)} 字。")
            else:
                st.warning("请先上传文件或粘贴文本！")
    
    if st.session_state.novel_content:
        st.info(f"💡 系统内存中已有 {len(st.session_state.novel_content)} 字的小说原稿供提取。")

# 面板2：系统工作流控制台
st.markdown("### ⚙️ 编剧工作流控制台")
if "global_setting" not in st.session_state:
    st.session_state.global_setting = "暂无全局设定，请先执行第1轮提炼，然后把觉得好的设定复制到这里备用。"

with st.expander("🧠 全局驱动卡与设定库 (允许人工修改)", expanded=False):
    st.session_state.global_setting = st.text_area(
        "在此修改或粘贴AI提炼的角色人设，后续生成将严格以此为准：", 
        value=st.session_state.global_setting, 
        height=200
    )
control_cols = st.columns(4)

with control_cols[0]:
    if st.button("🚀 第1轮：全局提炼", use_container_width=True):
        if not st.session_state.novel_content:
            st.warning("请先在上方录入小说内容！")
        else:
            prompt = f"【微短剧3.1启动】\n以下为当前系统内的小说内容，请执行【第1轮：全局提炼】。\n\n小说内容如下：\n{st.session_state.novel_content}"
            chat_with_ai(prompt)

with control_cols[1]:
    if st.button("🎬 第2轮：设计开场", use_container_width=True):
        chat_with_ai("已确认角色驱动卡。请执行【第2轮：开场手法设计】。")

with control_cols[2]:
    episode_num = st.number_input("集数", min_value=1, value=1, label_visibility="collapsed")
    plot_focus = st.text_input("本集剧情范围(防流水账):", placeholder="例：只写前30%女主被刁难的情节")
    
    if st.button(f"🎥 第3轮：生成第 {episode_num} 集 (带单镜自检版)", use_container_width=True):
        prompt = f"""
        【当前任务说明书：请仔细阅读以下生成标准】
        开始生成剧本 第{episode_num}集。本剧本直接喂给【即梦Seedance 2.0】生成视频。
        
        【核心要求】：
        1. 必须使用物理维度的确切描述，禁止极其复杂的连续动作。
        2. “可以保留描述基础人类情绪的副词（如：愤怒地、委屈地嘟嘴），但严禁使用跨物种或非自然现象的比喻（如：像太阳、像毛毛虫）
        【🚨 强制格式要求：单镜对比自检】
        为了绝对保证剧本符合 Seedance 2.0 的渲染要求，你在生成**每一个分镜**时，必须严格按照以下【先自检，后输出】的结构排版：

        ---
        【分镜 1 自检】
        1. 动作降维了吗？（是/否，将XX拆解为了XX）
        2. 存在比喻词或抽象名词吗？（是/否，将XX比喻转化为了XX物理光影/动作）
        3. 上下分镜内容能衔接上吗（是/否）
        【分镜 1 正文】
        场景：[时间/地点/光影]
        画面：[景别] + [主体视觉锚定] + [物理动作] + [具体环境介质]
        台词/音效：[具体台词/声音特征]
        ---
        （以此类推，每一个分镜都必须先写自检，再写正文！）


 ═══════════════════════════════════════
 时长感知校准系统（每次写分镜前必须调用）
═══════════════════════════════════════

你没有真实的视频时长感知能力，所以必须依靠以下换算尺
来锚定"我写的内容拍出来到底几秒"。

【1秒能装什么】
· 一个快速的表情变化（眼睛睁大）
· 一个简短动作（转头、伸手、按下按钮）
· 一句1-3个字的台词（"滚""你说呢？""嗯？"）
· 一个环境音效冲击（雷声、摔门声、手机震动）

【5秒能装什么】
· 一个完整的肢体动作（从拿起杯子→喝一口→放下）
· 一句5-12个字的短台词 + 说话者的一个表情反应
· 一个人从坐到站（或从站到走几步）
· 一个景别切换（从全景推到面部特写）

【10秒能装什么】
· 一段对话交锋（A说一句+B回一句+A的反应）
· 一组复杂连续动作（角色穿过房间→拿起某物→转身面对另一人→
  两人对视）
· 2-3个景别切换 + 一次环境氛围交代
· 一个完整的小型情绪转折（平静→触发→变化）

【14秒能装什么】
· 一轮2-3句的对话交锋 + 双方的表情/动作反应
· 一个包含铺垫→触发→爆发的完整微型事件
· 3-4个景别切换
· 一段包含环境互动的连续动作（如：在暴雨中跑→
  滑倒→爬起来→看到前方的人→表情剧变）

【写完分镜后的强制校验】
数你写了多少个"动作事件单元"：
- 1个独立动作/表情变化 = 约1-2秒
- 1句台词（按实际字数：5字≈1.5秒，10字≈3秒，20字≈5秒） 
- 1个景别切换 = 约0.5-1秒
- 1个环境/氛围描写= 约1-2秒

把所有单元时长加起来 = 这个分镜的真实预估时长。
标注的时长与实算时长偏差不得超过±2秒。
如果你标了10秒但内容只够5秒，要么补内容，要么改标注。

═══════════════════════════════════════
第五层：分镜格式与密度标准
═══════════════════════════════════════
【分镜XX】（XX秒 | 情绪：XXX）
 场景：地点 · 时间（早|中|晚） · 天气 · 光线 【每一个剧本分镜都得描述场景，禁止同上!!】
画面+台词：[必须满足以下密度要求，台词/OS：[如有。标注角色名。内心OS用（）标注。] ] 

衔接点：[本镜最后一帧 → 下一镜接入方式] -
---

【分镜内容密度铁律】

一个分镜如果标注10-14秒，其画面描写中必须包含：

必须有的：
· ≥3个连续的动作事件（不是一个动作的文学展开，
  是3个先后发生的不同事件）
· ≥4次景别/运镜变化（如：全景→特写、跟拍→固定机位、
  俯拍→平视）
· ≥1个具体的环境/声音细节
· ≥1个角色微表情或身体细节

如果有台词：
· 台词字数必须与标注时长匹配（3字≈1.5秒，8字≈3秒，
  12字≈5秒）
· 说话的同时必须有动作或表情伴随，禁止"站着说话"

---

对比示范：同一场戏，稀薄版 vs 饱满版

场景：女主发现男主和女配在一起

❌ 稀薄版（标了10秒但内容只够3秒）：
"女主站在门口，看到男主和女配坐在一起。
她的表情瞬间僵住，眼眶泛红。"
（问题：只有1个动作事件——"看到"，1个表情——"僵住泛红"。
这是一张静态图片，不是10秒的视频。）

✅ 饱满版（内容真正撑满10秒）：
"走廊尽头，女主端着两杯咖啡转过拐角——Loss
脚步在包厢玻璃门外顿住。（全景：走廊透视纵深，
她的身影映在玻璃门上形成半透明倒影）
咖啡杯盖缝渗出的热气在她指尖上方飘散。
玻璃门内，男主侧身坐着，正低头看着女配的手机屏幕，
女配的手搭在他肩上。两个人都在笑。
（景别切到女主面部特写）她嘴角的弧度还维持着
三秒前跟前台打招呼时的角度——卡在那个位置没收回来。
右手的咖啡杯微微倾斜，一滴咖啡从杯沿溢出来，
顺着杯壁淌到她的虎口上。她没感觉到。
（切到她的手部特写）咖啡淌过虎口，浸入指缝。
左手不自觉地收紧了另一杯的杯身，纸杯微微变形、
发出轻微的'咯吱'一声。
走廊尽头消防指示灯的绿光一明一灭，
把她凝固的影子投在地砖上，一闪、一闪。"

（拆解：动作事件①脚步顿住②看到门内画面③嘴角凝固④咖啡溢出
⑤手收紧纸杯变形；景别变化3次：走廊全景→面部特写→手部特写；
环境细节：热气、消防灯绿光明灭；声音细节：纸杯咯吱声。
实算时长：确实约10-14秒。）

---

禁止出现的情况：
· 标注10秒但只写了1-2个动作事件 → 立即补到≥3个
· 整个分镜都是一个静态画面的文学描写 → 必须加入时间流动
  （先发生什么→然后发生什么→接着发生什么）
· 角色"站着说话"——说台词时身体在做什么？手在哪？
  眼睛在看哪？必须同步描写。


        【剧情进度锚定】：
        {plot_focus if plot_focus else '根据原文合理推进单集容量。'}
        
        【核心人设依据】：
        {st.session_state.global_setting}
        
        小说原文参考：\n{st.session_state.novel_content}
        """
        chat_with_ai(prompt)


with control_cols[3]:
    if st.button("🔍 第4轮：原著对比自检", use_container_width=True):
        check_prompt = f"""
        请严格执行【第4轮：自检与优化】。
        对比刚刚生成的剧本与我上传的小说原文，针对每一个剧本分镜进行详细的检查。
        1. 调用敌对视角攻击（普通观众、竞品编剧、原著粉）。
        2. 进行量化打分。
        3. 7分以下的项目必须立即给出修改版！
        
        小说原文参考：
        {st.session_state.novel_content}
        三个敌对视角攻击：

视角1——普通观众：
"有没有哪里跳了？有没有哪句台词听着不像人话？
有没有哪个角色突然性格变了？有没有哪段画面描写
我读完脑子里没画面？"

视角2——竞品编剧：
"哪场戏是我见过一百遍的套路？哪个转折我猜到了？
哪里可以更狠/更细/更准？"

视角3——原著粉：
"哪个角色OOC了？哪句台词不是这个人会说的？
哪个情节被改变了因果关系？"

量化打分（每项1-10分）：
- 角色一致性（台词+行为是否符合驱动卡）：___
- 画面具象度（每个分镜是否有具体的不寻常细节）：___
- 台词活人感：___
- 因果链完整度：___
- 情绪过山车强度：___
- 开场15秒留人率预估：___
- 上下镜衔接流畅度：___
- 无旁白叙事清晰度：___
- 时长准确度（标注时长与内容实算时长偏差是否≤±2秒）：___
- 分镜密度（每个10-14秒分镜是否≥3个动作事件+≥2次景别变化）：___
- 视觉翻译完成度（是否有任何一处在用台词替代画面叙事）：___
7分以下的项目必须立即修改并输出修改版。     
        """
        chat_with_ai(check_prompt)
st.divider()

# ==========================================
# 6. 对话历史展示区
# ==========================================
col_title, col_download = st.columns([3, 1])
with col_title:
    st.subheader("💬 创作记录面板")
with col_download:
    # 如果系统有最新生成的回复，展示下载按钮
    if st.session_state.last_ai_response:
        st.download_button(
            label="💾 将最新结果下载为 TXT",
            data=st.session_state.last_ai_response,
            file_name="微短剧生成_或_自检结果.txt",
            mime="text/plain",
            use_container_width=True
        )

# 过滤掉系统提示词，只展示用户和AI的对话
for msg in st.session_state.messages:
    if msg["role"] != "system":
        with st.chat_message(msg["role"]):
            st.markdown(msg["content"])

# 允许用户自由输入修改指令（如：只改第X集/优化台词等）
if user_input := st.chat_input("自由模式：输入如'只优化台词'、'修改第3个分镜，让他更冷血一点'..."):
    chat_with_ai(user_input)
