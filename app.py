import streamlit as st
from openai import OpenAI
import os

# ==========================================
# 1. 系统提示词
# ==========================================
SYSTEM_PROMPT = """
【微短剧生成 3.1 系统指令】
═══════════════════════════════════════
第零法则：视觉翻译（一切规则之上的规则）
═══════════════════════════════════════

小说是给耳朵的——读者靠文字在脑中自己生成画面。
剧本是给眼睛的——观众只能看到你拍给他看的东西。

所以你的工作不是"把小说搬进分镜"。
你的工作是——

把小说用文字"告诉"读者的一切，
全部翻译成摄像机能拍到的画面,并用人物台词给观众解惑！
例如：
【路人甲劝告路人乙不要大声说话，被附近的丧尸听到，会有危险】
文字：（如果单纯只给路人甲劝告路人乙的台词，观众就带入不了角色面临的险境，因此需要给出丧尸在附近游走的镜头画面
这样可以让观众意识到他们处于险境之中）
画面：(如果只给出画面，让观众自己去理解，也会缺失效果，只看画面，如果路人甲不说这句台词，路人乙就不会知道）
【文字】+【画面】:因此正确的做法是两者结合，给到丧尸镜头渲染氛围，然后角色说台词让观众带入路人甲的立场

禁止对角色OOC，人物的台词、行为、举止都必须符合小说里的人设，绝不能做出违背角色性格的任何行为和说话方式！人物允许台词\内心OS展示，这是观众了解角色性格的重要途径之一!
台词(内心OS)不仅是信息传递，更是性格的声纹。怎么说、说什么、为什么说，都泄露了角色的内心，因此一定不能遗漏角色之间的互动画面！
在给核心角色编写每一句台词的时候都要参考【角色驱动卡】

这是一条凌驾于所有其他规则之上的法则。
如果你不确定该怎么写某个分镜，回来读这一条。

---

【翻译铁律】

铁律一：小说的"叙述"必须翻译为"动作流"

小说可以写"他实力强大，连杀十只异兽"——
一句话，十个字，读者自己脑补画面。

剧本里这十个字等于零。
你必须翻译成观众真正看到的东西：

✅ 正确翻译：
"他单手举枪，枪口闪光——（切）最近一只异兽脑袋
炸开黑色体液溅射——（切）他侧身闪过第二只扑来的利爪、
枪托反砸在它下颚上——（切）第三只从背后逼近、
他头都没回、枪伸到腋下朝后开了一枪——（切）
身后传来重物倒地的闷响，地面震了一下。
他甩了一下枪管上沾的黑色液体、单手退弹壳、
弹壳弹起来在半空转了两圈，叮地落在地上弹了一下。
硝烟从枪口升起来。他吹了一口气把烟吹散。"

这才是观众看到的"实力强大"。
不是角色自己说"我真厉害"，是画面本身让观众觉得厉害。

❌ 绝对禁止：
让角色站着念出小说原文里的叙述性内容。
比如角色说"一枪一个，我真是神勇无比"——
如果画面上没有任何打斗过程，观众看到的就是
一个人站在空地上自吹自擂。这不是爽，这是尬。

铁律二：小说的"心理描写"必须翻译为"身体（表情）反应搭配人物内心OS"

小说可以写"她心如刀绞，回忆起他们曾经的美好"——
剧本里这句话等于零，摄像机拍不到"心如刀绞"。

✅ 正确翻译：
"她手里攥着那张旧照片——拇指指腹压在照片上
他的脸的位置上，来回蹭了两下。然后指尖一用力，
照片从中间折出一道白印。她把照片翻扣在桌上，
手掌压住，深吸一口气。吸到一半卡住了，内心OS：“再也回不去了！”。
肩膀抖了一下，只一下，然后僵住不动了。"

❌ 绝对禁止：
角色直接说出内心状态不加情感演绎。
"我好心痛""我忘不了他""我现在非常愤怒"
——没有人在真正痛的时候会用完整句子描述自己在痛。

铁律三：小说的"设定/背景交代"必须翻译为"环境展示"

小说可以写"这是一个末世废土世界，
文明已经崩塌了二十年"——

✅ 正确翻译：
"（远景）灰绿色的天空下，一栋歪斜的高楼上
半截可口可乐广告牌吊着摇晃，铁皮刮着墙面
发出刺耳的吱嘎声。楼下街道上一辆翻倒的校车
车顶长满了苔藓，锈穿的车底盘有一窝鸟在筑巢。
镜头缓缓下移——一只野猫从校车破窗户里跳出来，
叼着一根人的指骨。"

观众在5秒内就理解了"末世、废土、崩塌很久了"，
不需要任何旁白解释，不需要角色说"这个世界完蛋了"。

❌ 绝对禁止：
用角色对话交代世界观。
"你知道吗，自从二十年前灾变发生后……"
——没有人会跟同一个世界里的人解释两个人都知道的背景。

铁律四：台词的正确用法——是观众解读剧本的桥梁
谁、在什么情境下、对谁、带着什么潜藏目的、用何种方式、说出了哪些字面内容，并最终服务于什么样的故事与主题
台词在剧本中的作用：

1. 字面含义（信息层）
这是台词最直接的功能：传递事实、说明情况、回答问题。比如“钥匙在门垫下面”，就是告诉对方一个具体信息。但真正让台词“有含义”的，是下面这些更重要的层面。

2. 潜台词（心理与真实意图层）
这是台词最核心的魅力所在。人物嘴上说的，往往不等于心里想的。潜台词就是隐藏在字面背后、人物真正想表达或掩饰的意思。理解潜台词，才算真正听懂了台词。
例子：一个人对伴侣说“你今晚又加班啊”，字面是询问事实。潜台词可能是：“我很孤独，希望你陪我”或“我不高兴你总把工作放在第一位”。
在好故事里，观众享受的就是“听出”潜台词的感觉。

3. 性格塑造层
台词是人物性格的标签。不同身份、性格的人，对同一件事的表达完全不同。
粗鲁的壮汉：“滚开！”
礼貌的管家：“先生，或许您该考虑换个位置了。”
天真的孩子：“叔叔，你挡住我看云了。”
通过台词的习惯用词、语气、句式（长短、文雅还是粗俗），读者/观众能立刻感知人物形象。

4. 人物关系层
台词能定义并动态展示人物之间的权力距离、亲疏程度和情感状态。
权力：上级用命令句，下级用请求句。比如“把报告给我” vs “请您过目报告”。
亲疏：爱人之间说“别吃太辣，你胃不好”，是关心。同一个词从陌生人嘴里说出来，就成了冒犯。
关系变化：两个朋友从“嘿，哥们儿”变成“你好，王先生”，台词本身就宣告了关系的疏远。

5. 推动情节与制造张力层
台词是情节的加速器或刹车片。
推动：一句“我知道凶手是谁”，立刻引发下一场追逐或对峙。
埋伏笔：“等我回来，就告诉你一切。”——让观众期待未来的真相时刻。
制造误会：“我其实很爱你，但我必须离开你。”（听起来像绝情，其实是牺牲），推动后续的误解与和解。

6. 主题与象征层
在某些富有文学性或思想性的作品中，台词会直接点题，或成为贯穿全篇的象征符号。
《教父》：“我要给他一个无法拒绝的提议。”——这句话反复出现，成为权力、家族和生存法则的象征。
《黑暗骑士》：“你要么作为英雄死去，要么活着看自己变成反派。”——这句话直接揭示了电影关于牺牲、混乱与道德的双重主题。

【视觉翻译的核心公式】

对每一段要改编的小说原文，执行以下翻译流程：

第一步——识别原文类型：
这段原文是在"告诉"读者什么？
A. 告诉读者发生了什么事 -> 翻译为动作流
B. 告诉读者角色的感受 -> 翻译为身体反应
C. 告诉读者世界观/背景 -> 翻译为环境展示
D. 告诉读者角色的能力/身份 -> 翻译为能力展示的动作场景
E. 告诉读者人物关系 -> 翻译为两人互动时的空间距离/
   肢体语言/视线交汇方式

第二步——翻译：
把"告诉"转化为"展示"。
问自己："如果观众是个聋子、看不到任何字幕，
只看画面，他能不能理解这段在讲什么？"
如果不能 -> 画面还不够，继续加可见的动作/环境信息。

第三步——最后考虑台词：
画面已经搭好舞台了
在用台词搭配角色情绪直观展现，让观众更有代入感。
如果有 -> 加上去。
如果没有 -> 就不加。




═══════════════════════════════════════
灵魂锚定（在写任何一个字之前，先读完这段）
═══════════════════════════════════════

你不是在"把小说改成剧本"。
你是在替这些角色活一遍。

他们有自己的脾气、自己的说话方式、自己在害怕时
会不自觉做的小动作。你的工作不是安排他们说什么做什么，
而是把他们扔进那个场景里，然后老老实实地记录——
他们自己会说什么，会做什么。

如果你写出来的画面，闭上眼睛脑子里看不见具体的图像，
那这段描写就是废的，重写。

产品规格：每集严格120-180秒 | 无第三人称旁白 | 集集强钩子。

═══════════════════════════════════════
第一层：五条创作铁律
═══════════════════════════════════════

①【人设即法律】
角色的性格、说话方式、行为逻辑必须95%忠于原著。
每次写角色的台词或行为前，必须先在脑中回看该角色的驱动卡。
自检问句："原著里这个人遇到类似情况会怎么做？我写的偏了没有？"
如果偏了，改回来。角色可以在剧情推进中成长变化，但变化的方向和节奏必须和原著一致。
严禁为了"好看""戏剧性""节奏需要"而让角色做出违反本性的事。

②【外化】
一切"想、觉得、心痛、暗爽"必须转化为可拍摄的具体画面。
允许角色用【第一人称内心OS】展现性格，严禁第三人称旁白。

③【伏笔】
每一个重大转折（"果"）之前，必须存在至少一个1-5秒的视觉/听觉微伏笔（"因"）。
伏笔可以极微弱，但不能缺失。观众不看原著时因果链必须完全成立。

④【潜台词】
角色嘴上说的话与真实意图之间必须存在缝隙。
台词传递表面意思，身体泄露真相。

⑤【钩子铁律】
前15秒必须制造具体的疑问或情绪冲击。
每集结尾必须制造"不看下一集不行"的未完成悬念。
集内至少一次情绪急转。


═══════════════════════════════════════
第四层：题材引擎（根据情绪需要选用）
═══════════════════════════════════════

【需要观众爽】-> 弹簧法：
先用物理细节把尊严按到最低（支票踩进泥水、手被高跟鞋碾出血），
再用零台词的降维打击反杀（直接掏出收购合同/保镖破门）。
反派表情三段崩塌：嚣张->错愕->崩塌。

【需要观众心动】-> 磁铁法：
心动靠空间入侵（壁咚时呼吸交缠）和不受控生理反应
（喉结滑动、耳根充血、视线不受控下移到嘴唇）。
气氛到最高点时被打断，制造未完成的痒感。

【需要观众虐】-> 错位法：
利用信息差让观众比角色知道更多（男主冷笑"她装可怜"
-> 画面切到女主暴雨中签病危通知书）。
不要让角色哭喊，让角色用极平静的动作粉碎定情信物。

【需要观众紧张】-> 橡皮筋法：
异象->虚惊一场（观众松气）-> 松懈的下一秒从视线死角
爆出真正威胁（双倍暴击）。
危机具象化为持续存在的视觉锚点（倒计时/黑线蔓延/逼近的影子）。

【需要观众笑】-> 错位法：
在最严肃场景做最接地气的事。人物立Flag下一秒被物理击飞。
极致反差时刻可使用【角色Chibi风格化】，须标注。

"""

SYSTEM_PROMPT = SYSTEM_PROMPT.strip()

# ==========================================
# 2. 页面与会话状态初始化
# ==========================================
st.set_page_config(
    page_title="微短剧3.2 剧本生成系统",
    page_icon="🎬",
    layout="wide"
)

if "messages" not in st.session_state:
    st.session_state.messages = [
        {"role": "system", "content": SYSTEM_PROMPT}
    ]
if "novel_content" not in st.session_state:
    st.session_state.novel_content = ""
if "last_ai_response" not in st.session_state:
    st.session_state.last_ai_response = ""
if "chapter_analysis" not in st.session_state:
    st.session_state.chapter_analysis = ""
if "selected_segments" not in st.session_state:
    st.session_state.selected_segments = ""
if "global_setting" not in st.session_state:
    st.session_state.global_setting = (
        "暂无全局设定, 请先执行第1轮提炼, "
        "然后把觉得好的设定复制到这里备用。"
    )

# ==========================================
# 3. 侧边栏
# ==========================================
with st.sidebar:
    st.header("⚙️ API 配置中心")
    api_key = st.text_input(
        "输入 API Key",
        type="password",
        help="第三方API平台的密钥"
    )
    base_url = st.text_input(
        "接口地址 (Base URL)",
        value="https://yunwu.ai/v1/"
    )

    st.subheader("🤖 模型选择")
    model_options = [
        "deepseek-chat",
        "deepseek-reasoner",
        "gpt-4o",
        "claude-3-5-sonnet-20241022",
        "✍️ 自定义模型",
    ]

    selected_model = st.selectbox("选择常用模型", model_options)

    if selected_model == "✍️ 自定义模型":
        model_name = st.text_input(
            "请输入中转站对应的模型ID",
            value="deepseek-chat",
            help=(
                "请参考 yunwu.ai 或你的中转站后台支持的模型名称列表, "
                "例如: gpt-4-turbo, claude-3-opus-20240229 等"
            )
        )
    else:
        model_name = selected_model

    st.info(f"当前生效模型: **{model_name}**")

    st.markdown("---")
    st.header("🧠 全局记忆管理")
    st.caption("如果你发现AI遗忘了, 可以在此点击清空重置。")
    if st.button("🗑️ 清空所有对话记忆与文本", use_container_width=True):
        # 1. 恢复初始系统提示词
        st.session_state.messages = [
            {"role": "system", "content": SYSTEM_PROMPT}
        ]
        # 2. 清空步骤二的分析内容
        st.session_state.chapter_analysis = ""
        st.session_state.selected_segments = ""
        
        # 3. 【新增】彻底清空小说原稿内存
        st.session_state.novel_content = ""
        
        # 4. 【新增】还原全局人设设定库
        st.session_state.global_setting = (
            "暂无全局设定, 请先执行第1轮提炼, "
            "然后把觉得好的设定复制到这里备用。"
        )
        
        # 5. 【新增】清空最后一次AI的回复记录
        st.session_state.last_ai_response = ""
        
        st.success("🧹 所有小说文本、人设记忆和对话已彻底清空，防止干扰！")
        
        # 强制页面重新加载，让文本框立刻变空白
        st.rerun()
        
    st.markdown("---")  # 将这里的缩进改成4个空格，和下一行对齐
    st.header("💾 快捷导出")
    if st.session_state.get("last_ai_response"):
        st.download_button(
            label="📥 下载最新 TXT 文本",
            data=st.session_state.last_ai_response,
            file_name="微短剧生成脚本.txt",
            mime="text/plain",
            use_container_width=True,
            type="primary" # 让侧边栏按钮变醒目
        )
    else:
        st.button("📥 暂无内容可下载", disabled=True, use_container_width=True)

# ==========================================
# 4. 核心对话逻辑
# ==========================================
def chat_with_ai(prompt):
    if not api_key:
        st.error("请先在左侧边栏填写 API Key!")
        st.stop()

    client = OpenAI(api_key=api_key, base_url=base_url)

    st.session_state.messages.append({"role": "user", "content": prompt})

    with st.chat_message("user"):
        st.markdown(prompt)

    with st.chat_message("assistant"):
        message_placeholder = st.empty()
        full_response = ""

        try:
            response = client.chat.completions.create(
                model=model_name,
                messages=st.session_state.messages,
                stream=True,
                temperature=0.7
            )
            for chunk in response:
                if chunk.choices[0].delta.content is not None:
                    full_response += chunk.choices[0].delta.content
                    message_placeholder.markdown(full_response + "▌")
            message_placeholder.markdown(full_response)
        except Exception as e:
            st.error(f"API 请求失败: {e}")
            st.session_state.messages.pop()
            return

    st.session_state.messages.append(
        {"role": "assistant", "content": full_response}
    )
    st.session_state.last_ai_response = full_response
    
    # 强制刷新页面状态，让左侧边栏的下载按钮立刻获取到这部分最新文本
    st.rerun()
# ==========================================
# 5. 主工作区
# ==========================================
st.title("🎬 影视化视觉翻译引擎 V3.2")
st.markdown(
    "> **小说是给耳朵的, 剧本是给眼睛的。** "
    "严格按照视觉翻译法则执行。新增: 章节拆解与取舍决策, 杜绝流水账。"
)

# ========== 面板1: 导入小说 ==========
with st.expander("📝 步骤一: 导入小说章节原文", expanded=True):
    col1, col2 = st.columns([1, 1])
    with col1:
        uploaded_file = st.file_uploader("选择本地 TXT 文件", type=["txt"])
    with col2:
        pasted_text = st.text_area(
            "或者在此处直接粘贴章节内容", height=150
        )

    current_input = ""
    if uploaded_file is not None:
        current_input = uploaded_file.getvalue().decode("utf-8")
    elif pasted_text:
        current_input = pasted_text

    btn_col1, btn_col2, _ = st.columns([2, 2, 4])
    with btn_col1:
        if st.button(
            "📥 录入为当前处理章节 (覆盖旧内容)", type="primary"
        ):
            if current_input:
                st.session_state.novel_content = current_input
                st.session_state.chapter_analysis = ""
                st.session_state.selected_segments = ""
                st.success(
                    f"成功录入! 当前待处理小说共计 "
                    f"{len(st.session_state.novel_content)} 字。"
                )
            else:
                st.warning("请先上传文件或粘贴文本!")
    with btn_col2:
        if st.button("➕ 追加新章节 (保留之前内容)"):
            if current_input:
                st.session_state.novel_content += "\n\n" + current_input
                st.success(
                    f"追加成功! 当前系统内小说总计 "
                    f"{len(st.session_state.novel_content)} 字。"
                )
            else:
                st.warning("请先上传文件或粘贴文本!")

    if st.session_state.novel_content:
        st.info(
            f"💡 系统内存中已有 "
            f"{len(st.session_state.novel_content)} 字的小说原稿供提取。"
        )

# ========== 全局设定 ==========
with st.expander("🧠 全局驱动卡与设定库 (允许人工修改)", expanded=False):
    st.session_state.global_setting = st.text_area(
        "在此修改或粘贴AI提炼的角色人设, 后续生成将严格以此为准:",
        value=st.session_state.global_setting,
        height=200
    )

# ========== 面板2: 章节拆解 (新增核心功能) ==========
st.markdown("---")
st.markdown("### 🔬 步骤二: 章节拆解与取舍决策 (新增关键步骤)")
st.caption(
    "这一步是解决AI把整章内容硬塞进剧本导致流水账的核心。"
    "AI会先分析这一章有哪些事件, 哪些值得拍, 哪些应该砍掉, "
    "然后你来做最终决策。"
)

with st.expander(
    "📋 章节拆解结果 (AI分析后自动填充, 你可以手动修改)",
    expanded=True
):
    st.session_state.chapter_analysis = st.text_area(
        "章节事件拆解 (点击下方按钮让AI分析, 或手动填写):",
        value=st.session_state.chapter_analysis,
        height=200,
        help="这里会列出本章的所有事件单元以及AI对每个事件的取舍建议"
    )

    st.session_state.selected_segments = st.text_area(
        "✅ 最终确认: 本集要拍的内容 (从上方分析中选取或自己写):",
        value=st.session_state.selected_segments,
        height=100,
        placeholder=(
            "例如: 只拍事件2(女主被婆婆当众羞辱)和事件3"
            "(男主冷眼旁观), 其他全部砍掉。"
            "本集核心情绪: 憋屈->爆发前的最后一根稻草。"
        )
    )

analysis_cols = st.columns(2)
with analysis_cols[0]:
    if st.button(
        "🔬 AI自动拆解本章 (推荐先点这个)",
        use_container_width=True,
        type="primary"
    ):
        if not st.session_state.novel_content:
            st.warning("请先在上方录入小说内容!")
        else:
            analysis_prompt = (
                "【任务: 章节拆解与取舍决策】\n\n"
                "你现在是一个资深微短剧编剧总监。"
                "在动笔写任何一个分镜之前, 你必须先完成以下分析工作。\n\n"
                "请阅读以下小说章节原文, 然后严格按照下面的格式输出分析结果:\n\n"
                "═══════════════════════════════════════\n"
                "一、事件拆解 (把这一章拆成独立的事件单元)\n"
                "═══════════════════════════════════════\n\n"
                "把这一章按照发生了什么事拆成若干个独立事件。"
                "每个事件用以下格式:\n\n"
                "【事件1】一句话概括 (例: 女主在公司被上司当众训斥)\n"
                "- 涉及角色: XX、XX\n"
                "- 情绪价值: 这个事件能给观众带来什么情绪? (爽/虐/心动/紧张/笑/无感)\n"
                "- 视觉潜力: 这个事件拍出来好不好看? 有没有强画面? (高/中/低)\n"
                "- 信息功能: 这个事件是交代背景/推进主线/建立人设/埋伏笔/纯过渡?\n"
                "- 耗时预估: 如果要拍好这个事件, 大约需要几秒? "
                "(注意微短剧一集只有120-180秒)\n\n"
                "【事件2】......\n(以此类推)\n\n"
                "═══════════════════════════════════════\n"
                "二、取舍建议\n"
                "═══════════════════════════════════════\n\n"
                "基于以上分析, 给出明确的取舍建议:\n\n"
                "🟢 必须保留 (核心事件, 删了观众看不懂):\n"
                "- 事件X: 理由\n"
                "- 事件X: 理由\n\n"
                "🟡 建议精简 (有用但可以压缩成1-2个镜头带过):\n"
                "- 事件X: 建议怎么压缩\n\n"
                "🔴 建议砍掉 (对视觉叙事没有贡献, 或可以用其他方式替代):\n"
                "- 事件X: 理由, 以及如果观众需要知道这个信息, "
                "可以用什么视觉方式替代\n\n"
                "═══════════════════════════════════════\n"
                "三、单集容量评估\n"
                "═══════════════════════════════════════\n\n"
                "按照120-180秒的单集时长, 这一章的内容:\n"
                "- 适合拆成几集?\n"
                "- 如果只做1集, 建议聚焦哪些事件?\n"
                "- 本集的核心情绪曲线应该是什么? (例: 压抑->爆发->悬念)\n"
                "- 本集的一句话卖点是什么? (观众看完这一集会跟朋友说的那句话)\n\n"
                "═══════════════════════════════════════\n\n"
                "小说原文如下:\n"
                f"{st.session_state.novel_content}\n\n"
                "角色设定参考:\n"
                f"{st.session_state.global_setting}"
            )
            chat_with_ai(analysis_prompt)
            st.info(
                "💡 请阅读AI的分析结果, 然后将你认可的取舍方案"
                "复制到上方的最终确认框中, 再进入下一步生成剧本。"
            )

with analysis_cols[1]:
    if st.button(
        "🎯 我已确认取舍方案, 锁定本集内容",
        use_container_width=True
    ):
        if st.session_state.selected_segments:
            st.success(
                "✅ 取舍方案已锁定! 现在可以进入下方的剧本生成流程。"
            )
        else:
            st.warning("请先在上方最终确认框中填写本集要拍的具体内容!")

# ========== 面板3: 编剧工作流控制台 ==========
st.markdown("---")
st.markdown("### ⚙️ 步骤三: 编剧工作流控制台")

control_cols = st.columns(4)

with control_cols[0]:
    if st.button("🚀 第1轮: 全局提炼", use_container_width=True):
        if not st.session_state.novel_content:
            st.warning("请先在上方录入小说内容!")
        else:
            prompt = (
                "【微短剧3.1启动】\n"
                "以下为当前系统内的小说内容, "
                "请执行【第1轮: 全局提炼】。\n\n"
                "小说内容如下:\n"
                f"{st.session_state.novel_content}"
            )
            chat_with_ai(prompt)

with control_cols[1]:
    if st.button("🎬 第2轮: 设计开场", use_container_width=True):
        chat_with_ai(
            "已确认角色驱动卡。请执行【第2轮: 开场手法设计】。"
        )

with control_cols[2]:
    # 1. 修改 UI：两个数字输入框控制集数
    sub_col1, sub_col2 = st.columns(2)
    with sub_col1:
        start_episode_num = st.number_input(
            "起始集号", min_value=1, value=1, help="生成的剧本从第几集开始命名"
        )
    with sub_col2:
        target_episodes_count = st.number_input(
            "拆分集数", min_value=1, max_value=10, value=1, help="这段剧情你想拆成几集写？集数越多，细节和台词越丰富！"
        )

    if st.button(
        f"🎥 第3轮: 连续生成 {target_episodes_count} 集",
        use_container_width=True
    ):
        
        # ===== 核心：将“聚焦指令”与“拆分降速指令”完美结合 =====

        # 第一步：构建【边界与平滑过渡指令】
        focus_instruction = ""
        if st.session_state.selected_segments:
            focus_instruction = (
                "════════════════════════════════════════════\n"
                "⚠️ 【聚焦放大镜与平滑过渡指令】\n"
                "════════════════════════════════════════════\n"
                "用户已经完成了章节拆解，本次生成重点聚焦以下内容：\n"
                f"{st.session_state.selected_segments}\n\n"
                "【执行铁律】:\n"
                "1. 核心锚定：不要向后超纲推进剧情，必须把算力用来深挖上述事件的细节！\n"
                "2. 允许滑行过渡：为了避免画面像PPT一样生硬跳跃，强烈建议你从【小说原文参考】中，提取人物的入场走位、动作前摇、周遭人的初始状态，作为承上启下的过渡镜头，让核心事件自然发生。\n"
            )
        else:
            focus_instruction = (
                "════════════════════════════════════════════\n"
                "⚠️ 【当前未指定聚焦范围，将处理全部原文】\n"
                "════════════════════════════════════════════\n"
            )

        # 第二步：构建【剧集拆分指令】
        split_instruction = (
            "════════════════════════════════════════════\n"
            "⏳ 【剧集拆分指令】\n"
            "════════════════════════════════════════════\n"
            f"请将上述情节，合理规划并扩展为完整的 **{target_episodes_count} 集** 微短剧剧本。\n"
            f"从 第 {start_episode_num} 集 开始，一直写到 第 {start_episode_num + target_episodes_count - 1} 集。\n\n"
            "【降速与细节挖掘】:\n"
            "既然分配了多集，必须放慢叙事节奏！原著中的心理博弈、细微的肢体拉扯必须全部保留并转化为视觉动作。每集时长120-180秒，细节必须拉满。\n"
        )

        # 第三步：组装最终 Prompt (引入“先推演后输出”和“示范替代公式”策略)
        prompt = (
            "【当前任务说明书：请仔细阅读以下生成标准】\n"
            "本剧本直接喂给【即梦Seedance 2.0】生成视频。\n\n"
            f"{focus_instruction}\n"
            f"{split_instruction}\n\n"
            "═══════════════════════════════════════\n"
            "【剧本生成流程：先推演，后输出】\n"
            "═══════════════════════════════════════\n"
            "为了彻底杜绝“干瘪的动作流水账”和“毫无逻辑的AI味台词”，请严格按以下两阶段执行：\n\n"
            "=== 阶段一：情绪与台词沙盘推演（不写分镜，先理逻辑） ===\n"
            "在动笔前，请先简要分析这部分剧情的核心交锋点：\n"
            "1. 双方此时的真实心理动机是什么？\n"
            "2. 提取并提炼原著中的关键台词（遵循角色驱动卡），如果原著没台词，请根据情绪补充符合其身份的台词。必须符合角色人设！\n\n"
            "=== 阶段二：一气呵成连续生成剧本 ===\n"
            "推演完成后，开始连续输出剧本。请忘掉凑字数，完全沉浸在刚才推演的情绪中。\n"
            "【动作-反应连环铁律】：台词不是单口相声！A说完台词或做完动作，必须紧接着写B听到/看到后的生理反应（眼神、肢体僵硬等），然后再给出B的台词！\n\n"
            "【优质分镜对标范例（请绝对模仿此密度与节奏）】：\n"
            "错误示范（AI味、干瘪跳跃）：男主很愤怒，打倒反派。台词：“你找死！”\n"
            "正确示范（画面饱满、反应连环）：男主下颚线瞬间绷紧（微表情）。他没有任何废话，右手抄起桌上的烟灰缸（动作），跨步上前，猛地砸在反派侧脸上（动作）。“砰”的一声闷响伴随玻璃碎裂（音效）。反派被砸得踉跄后退，捂住流血的额头，眼神充满不可置信（动作反馈）。男主（声音压得很低，带着极度危险的平静）：“再说一次，谁给你的胆子？”（台词结合情绪）\n\n"
            "【强制输出格式】：\n"
            "# 🎬 【第 X 集】\n"
            "【分镜 1】(10-15秒 | 情绪: XXX)\n"
            "场景: [地点] - [时间] - [光线]\n"
            "内容：[画面动作细节] + [台词结合情绪] + [对手的反应]\n"
            "衔接点: [本镜最后一帧 -> 下一镜接入方式]\n"
            "---\n"
            "(以此类推，严禁使用之前的自检问答打断分镜生成)\n\n\n"
            "【核心人设依据】:\n"
            f"{st.session_state.global_setting}\n\n"
            "小说原文参考(绝密细节库，务必深挖):\n"
            f"{st.session_state.novel_content}"
        )
        chat_with_ai(prompt)


with control_cols[3]:
    if st.button("🔍 第4轮: 原著对比自检", use_container_width=True):
        focus_check = ""
        if st.session_state.selected_segments:
            focus_check = (
                "\n\n额外检查项——聚焦范围遵守度:\n"
                f"用户指定本集只拍: {st.session_state.selected_segments}\n"
                "请逐个分镜检查: 是否有任何分镜在讲述指定范围以外的内容?\n"
                "如果有, 标记为🔴违规, 并建议删除或替换。\n"
            )

        check_prompt = (
            "请严格执行【第4轮: 自检与优化】。\n"
            "对比刚刚生成的剧本与我上传的小说原文, "
            "针对每一个剧本分镜进行详细的检查。\n"
            "1. 调用敌对视角攻击(普通观众、竞品编剧、原著粉)。\n"
            "2. 进行量化打分。\n"
            "3. 8分以下的项目必须立即进行修改。\n"
            f"{focus_check}\n\n"
            "小说原文参考:\n"
            f"{st.session_state.novel_content}\n\n"
            "五个敌对视角攻击:\n\n"
            "视角1——普通观众:\n"
            "有没有哪里跳了? 有没有感觉很懵? "
            "不知道故事讲的是什么内容? "
            "有没有哪句台词听着不像人话?\n"
            "有没有哪个角色突然性格变了? "
            "有没有哪段画面描写我读完脑子里没画面?\n\n"
            "视角2——竞品编剧:\n"
            "哪场戏是我见过一百遍的套路? 哪个转折我猜到了?\n"
            "哪里可以更狠/更细/更准?\n\n"
            "视角3——原著粉:\n"
            "哪个角色OOC了? 哪句台词不是这个人会说的? "
            "角色之间有没有互动? 角色塑造深不深刻?\n"
            "哪个情节被改变了因果关系?\n\n"
            "视角4——剪辑师:\n"
            "有没有哪个分镜剪辑节奏缺失? "
            "有没有真正完成向“摄影机视角”的底层认知转换？\n"
            "视角5——导演粉:\n"
            "有没有那段台词出现逻辑断层? "
            "如果闭上眼睛，只听该集的【音效】+【角色台词】+【内心OS】 "
            "三者构成的“声音轨道”有没有完整的因果逻辑链\n"
            "有没有出现说出或回答的台词内容与前一个问题或论述之间没有逻辑关联？\n\n"
            "量化打分(每项1-10分):\n"
            "- 角色一致性(台词+行为是否符合驱动卡): ___\n"
            "- 画面具象度(每个分镜是否有具体的不寻常细节): ___\n"
            "- 角色行为/台词真实感: ___\n"
            "- 因果链完整度: ___\n"
            "- 情绪过山车强度: ___\n"
            "- 开场15秒留人率预估: ___\n"
            "- 上下镜衔接流畅度: ___\n"
            "- 无旁白叙事清晰度: ___\n"
            "- 视觉翻译完成度"
            "(是否有任何一处只用了台词没进行画面呈现): ___\n"
            "- 聚焦范围遵守度(是否所有分镜都在指定范围内): ___\n\n"
            "═══════════════════════════════════════\n"
            "🚨 终极输出强制要求 —— 拒绝偷懒省略 🚨\n"
            "═══════════════════════════════════════\n"
            "在完成打分和修改决策后，你必须将【无需修改的满分分镜】与【修改后的分镜】按顺序完整拼接！\n"
            "严禁使用“分镜2-7保持原样”等省略语！严禁跳过任何一个分镜！\n"
            "你最后必须完完整整地输出这一集最终版的全部剧本正文（从分镜1一直到最后一个分镜），方便我直接复制！"
        )
        chat_with_ai(check_prompt)

st.divider()

# ==========================================
# 6. 对话历史展示区
# ==========================================
col_title, col_download = st.columns([3, 1])
with col_title:
    st.subheader("💬 创作记录面板")
with col_download:
    if st.session_state.last_ai_response:
        st.download_button(
            label="💾 将最新结果下载为 TXT",
            data=st.session_state.last_ai_response,
            file_name="script_output.txt",
            mime="text/plain",
            use_container_width=True
        )

for msg in st.session_state.messages:
    if msg["role"] != "system":
        with st.chat_message(msg["role"]):
            st.markdown(msg["content"])

if user_input := st.chat_input(
    "自由模式: 输入如 只优化台词 或 修改第3个分镜让他更冷血一点 ..."
):
    chat_with_ai(user_input)
